name: "Workflow commands"

on: [push]

jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            - name: "group logging"
              run: |
                echo "::group::My group"
                echo "Msg1"
                echo "Msg2"
                echo "::endgroup::"